from loader import bot
from telebot.types import Message
from states.user_states import UserStateInfo


# @bot.message_handler()
# def help_message(message: Message) -> None:
#     bot.send_message(message.from_user.id, "Сначала нужно авторизоваться в сервисе. Для этого наберите команду /survey")
